android {
            defaultConfig {
                applicationId = "org.nativescript.DemoAPL"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }